extends layouts/main.pug

block content
    .day
        ol.day__list
            li.day__item
                h1.title 일요일
                ul.todolist
                    li 할 일 1
                form.add-form
                    input.add-form__input(type="text" placeholder="할 일")
                    button.add-form__button 추가
            li.day__item
                h1.title 일요일
                ul.todolist
                    li 할 일 1
                form.add-form
                    input.add-form__input(type="text" placeholder="할 일")
                    button.add-form__button 추가
            li.day__item
                h1.title 일요일
                ul.todolist
                    li 할 일 1
                form.add-form
                    input.add-form__input(type="text" placeholder="할 일")
                    button.add-form__button 추가
            li.day__item
                h1.title 일요일
                ul.todolist
                    li 할 일 1
                form.add-form
                    input.add-form__input(type="text" placeholder="할 일")
                    button.add-form__button 추가
            li.day__item
                h1.title 일요일
                ul.todolist
                    li 할 일 1
                form.add-form
                    input.add-form__input(type="text" placeholder="할 일")
                    button.add-form__button 추가
            li.day__item
                h1.title 일요일
                ul.todolist
                    li 할 일 1
                form.add-form
                    input.add-form__input(type="text" placeholder="할 일")
                    button.add-form__button 추가
            li.day__item
                h1.title 일요일
                ul.todolist
                    li 할 일 1
                form.add-form
                    input.add-form__input(type="text" placeholder="할 일") 
                    //지난 날짜 빼고 input 보이게
                    button.add-form__button 추가
    button.type__button(type="button") 할 일
    // 버튼 클릭하면 할 일이 보이고 버튼 내용은 일기로 변함, 또 누르면 반대로
    // 로그인 안했으면 일기는 전부 자물쇠 처리 되어서 안보임
    button.calendar__button(type="button") 캘린더로 이동
