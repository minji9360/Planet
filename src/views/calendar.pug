extends layouts/main.pug

block content
    section
        a.prev__btn <
        .content
            .day
                h1.day__title 2일 월요일
                .day__content
                    // 그 날의 한 일/할 일
            .calendar
                h1.calendar__title 2021년 8월
                table.calendar__table
                    thead
                        tr.day__tr
                            th 일
                            th 월
                            th 화
                            th 수
                            th 목
                            th 금
                            th 토
                    tbody.date
        a.next__btn > 
    //- button.type__button(type="button") 할 일
    // 버튼 클릭하면 할 일이 보이고 버튼 내용은 일기로 변함, 또 누르면 반대로
    // 로그인 안했으면 일기는 전부 자물쇠 처리 되어서 안보임
    //- button.todolist__button(type="button") 할 일 목록으로 이동

